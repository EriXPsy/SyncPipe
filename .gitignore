__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.pytest_cache/
.coverage
htmlcov/
.ipynb_checkpoints/
*.log
venv/
.venv/
.vscode/
.idea/
*.swp

# Published papers are not redistributed in this repository (copyright).
# Fetch them from the publishers' DOI pages or the papers' OSF archives.
*.pdf

# Generated demo/analysis outputs (reproducible via syncpipe demo)
DEMO_REPORT.md
demo_results.json
design_control_audit.json
feature_status_table.csv
feature_table.csv
results.json
synchrony_existence_audit.json
SYNCPIPE_FAMILY_TREE.html
TABLE1_FEATURE_STATUS.tex
*_output.txt
*_summary.txt
*_full.txt

# Demo-generated artifacts (paper snapshots under artifacts/ remain tracked)
artifacts/demo_v1/
artifacts/ci_demo/

# Temporary debug probes (hardcoded external OSF paths; not part of the package)
scripts/_probe_loaders.py
scripts/_timing_probe.py

# Internal working/audit documents (S-5, maintainer red line: internal
# review/audit documents never ship in the public repo)
docs/V1_CLEANUP_PLAN.md
artifacts/reviewer_audit/

# --- Phase-0 cleanup: generated repo/run artifacts (not version controlled) ---
# Graphify tool outputs (repo root + package dir)
graphify-out/
syncpipe/graphify-out/
# Runtime log captures written inside the package dir by long runs
syncpipe/_logs/
# Pytest text capture at repo root (note: *_output.txt / *_summary.txt already
# covered above; pytest_out.txt ends in _out.txt so needs its own entry)
pytest_out.txt
# Generated analysis artifacts under artifacts/ (reproducible from scripts;
# distinct from the intentionally-tracked paper snapshots in artifacts/paper_lerique/)
artifacts/bakeoff/
artifacts/figures/
artifacts/incremental_value/
artifacts/realdata_full/
artifacts/timing_validation/
artifacts/timing_feature_audit.csv
artifacts/autocorr_robustness.json
artifacts/bizzego_convergence.json

# Local-only working materials, not redistributed in the public repo
# (round-5b cleanup, DECISION_LOG 2026-09-10): historical diagnostic
# scripts, archived figures/artifacts, and internal process notes.
archive/
experimental/scripts/
docs/V1_CLEANUP_PLAN.md
docs/SCRIPT_MAP.md
